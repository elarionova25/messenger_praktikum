import {LoginPage} from "./login";
import {expect} from "chai";
import sinon from "sinon";

describe('LoginPage', () => {
    it('should render', () => {
        // @ts-ignore
        new LoginPage();
    });
});